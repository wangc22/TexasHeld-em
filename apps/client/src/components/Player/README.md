# Player

Player seat display.

## Components

| Component | Description |
|---|---|
| `PlayerSeat.tsx` | Renders a single seat: avatar, player name, chip stack, hole cards (face-down for opponents), current bet, action badge (fold/check/raise overlay), and turn-active highlight |
