# Card

Playing card renderer.

## Components

| Component | Description |
|---|---|
| `PlayingCard.tsx` | Renders a single card face or face-down back; accepts `rank` and `suit` props; uses SVG icons from `public/icons.svg` |
