# Actions

Player action controls shown during a player's turn.

## Components

| Component | Description |
|---|---|
| `ActionPanel.tsx` | Renders fold / check / call / raise / all-in buttons; shows raise slider and bet-size presets; reads `ValidActions` from the game store and only enables legal actions |
