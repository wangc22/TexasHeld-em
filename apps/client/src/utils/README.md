# utils

Client-side utility functions.

## Files

| File | Description |
|---|---|
| `chipUtils.ts` | Converts a chip count to a human-readable denomination string (e.g. `1500` → `"1.5K"`); used in `PlayerSeat` and `BetDisplay` |
